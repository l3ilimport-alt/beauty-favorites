# ⚠️ קרא לפני שאתה נוגע בגיט של התיקייה הזו

עודכן: 2026-09-03

## התיקייה הזו אינה נקודת פרסום

הפרסום היחיד המותר הוא:

./deploy-catalog.command

הוא משבט את המאגר החי מחדש לתיקייה תחת הבית ודוחף משם. **הוא אינו משתמש
במנגנון הגיט שיושב כאן.**

## למה `git status` מראה כמעט 9,000 שינויים — וזה לא אומר כלום

המצביע המקומי (`HEAD`) יושב על גרסה `35a35cf` מ-24 ביוני, ששייכת ל**היסטוריה
יתומה**: 39 גרסאות שנוצרו מול המאגר הישן `sephora-favorites`, שאין להן שום אב
משותף עם המאגר החי. `git status` משווה מול המצביע הזה, ולכן המספר שהוא מציג
הוא רעש.

**ההשוואה האמיתית מול המאגר החי, שנעשתה ב-3.9.2026:**

| | |
|---|---|
| קיים כאן ואינו במאגר החי | **2 קבצים** — `upc_key.txt` ו-`deploy-catalog.command`, שניהם מוחרגים בכוונה |
| קיים במאגר החי ואינו כאן | **468 תמונות** — אף אחת אינה מוזכרת לא ב-`index.html` המקומי ולא בזה שחי באתר. יתומות |

**המסקנה: אין ולו קובץ עבודה אחד שלא הגיע לגיטהאב.**

לשחזור ההשוואה:

git -c core.quotePath=false ls-tree -r --name-only origin/main

## מה תוקן ב-3.9.2026

1. **`origin` הופנה** מ-`sephora-favorites` (מאגר מת ששימש דף הפניה) ל-`beauty-favorites` (החי).
2. **הותקן חוסם `pre-push`** — דחיפה מכאן הייתה דורסת את האתר.
3. **הוצאו שרידי העתקה מתוך `.git`:** שתי הפניות שבורות (`origin/main 2` ו-`main 3`)
   ש**הפילו את `git fetch`** בשגיאת `bad object`, ו-13 קובצי אינדקס כפולים.
   נשמרו ב-`~/סידור-מחשב-2026-09-02/שרידי-גיט-שהוצאו/`.
4. **גיבוי מלא של `.git` לפני הכל:** `~/סידור-מחשב-2026-09-02/git-ספורה-catalog-לפני-תיקון-2026-09-02.tar`

## המאגרים

| מאגר | תפקיד |
|---|---|
| `beauty-favorites` | **הקטלוג החי.** מגיש את beautyfavorites.co.il |
| `sephora-favorites` | **מת.** דף הפניה בלבד — אין לגעת |
| `sephora-backoffice` | הבק אופיס הבנוי |

**⚠️ פתוח:** במשימות של ספורה שורה 375 רשום `beauty-favorites-v2`, בעוד הסקריפט
מצביע ל-`beauty-favorites`. הסתירה טרם הוכרעה.
